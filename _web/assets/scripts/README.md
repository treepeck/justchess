Scripts in root folder are used by particular pages.
They can import arbitrary scripts from subfolders.

widget subfolder contains scripts that render an arbitrary UI elements.

chess subfolder contains chess logic needed for UI rendering purposes.
It's goal is to know about chess as less as possible.
